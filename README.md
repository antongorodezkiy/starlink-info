# Starlink Info app

Provides small app to get Starlink info from the local network.

## Init

```
python -m venv venv-pywebview
venv-pywebview\Scripts\activate
npm run init
pip install -r requirements.txt
npm i
npm run build
```
